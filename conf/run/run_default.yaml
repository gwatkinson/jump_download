# Options used in CLI
job_path: ??? # This should be provided by the user in the CLI, ??? is the correct way to do this, don't change it
max_workers: 8 # The maximum number of workers to use for multiprocessing
force: False
